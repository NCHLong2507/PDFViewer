<!DOCTYPE html>
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title Email Verification
    style.
      body {
        font-family: Arial, sans-serif;
        background-color: #f4f4f4;
        margin: 0;
        padding: 20px;
      }
      .container {
        max-width: 600px;
        margin: auto;
        background-color: #ffffff;
        padding: 30px;
        border: 1px solid #dddddd;
        border-radius: 8px;
      }
      .header {
        background-color: #000;
        color: #fff;
        display: flex;
        flex-direction: row; /* Logo và h1 trên cùng một hàng */
        align-items: center; /* Căn giữa theo chiều dọc */
      }
      .logo {
        height: 80px; /* Logo lớn */
        margin-right: 15px; /* Khoảng cách bên phải logo */
      }
      .header h1 {
        flex: 1; 
        margin: 0; 
        text-align: center; 
        font-size: 24px; 
      }
      .message {
        font-size: 16px;
        margin: 20px 0;
        text-align: center;
      }
      .verify-btn {
        display: block;
        width: 200px;
        margin: 20px auto;
        padding: 12px;
        background-color: #007bff;
        color: white;
        text-align: center;
        text-decoration: none;
        border-radius: 5px;
        font-weight: bold;
        transition: background-color 0.3s ease;
      }
      .verify-btn:hover {
        background-color: #0056b3;
      }
      .footer {
        margin-top: 30px;
        text-align: center;
        font-size: 12px;
        color: #888888;
      }
      .welcome {
        text-align: center;
      }
  body
    .container
      // HEADER
      .header
        img.logo(src="https://res.cloudinary.com/duiuvuxeu/image/upload/v1747154141/DSVlogo_pm7keh.jpg" alt="Designveloper Logo")
        h1 LuminPDF Demo

      // CONTENT
      .content
        h2.welcome Welcome, #{name}!
        .message
          p Thank you for signing up with us.
          p Please click the button below to verify your email address.

      // BUTTON
      a.verify-btn(href=verificationLink) Verify Email

      // FOOTER
      .footer
        p If you did not create this account, please ignore this email.